# MySQL（与 README 默认值一致时可省略）
MYSQL_HOST=127.0.0.1
MYSQL_PORT=3306
MYSQL_USER=root
MYSQL_PASSWORD=
MYSQL_DATABASE=argus

# 可选
# PORT=8080

# 公网访问密码。为空时不启用访问限制；配置后，内网/localhost 仍免认证，公网访问需输入此密码。
# ARGUS_PUBLIC_PASSWORD=

# 只有应用运行在可信反向代理后，且需要按 X-Forwarded-For 判断内外网时才开启。
# ARGUS_TRUST_PROXY=false
