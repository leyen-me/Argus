/** 与 `argus-renderer.ts` 中字符串保持一致 */
export const ARGUS_CONFIG_MODAL_OPEN = "argus:config-modal-open";
export const ARGUS_CONFIG_MODAL_CLOSE = "argus:config-modal-close";

/** saveConfig（如仪表盘统计/策略执行态）后广播，便于其它面板拉取最新配置切片 */
export const ARGUS_APP_CONFIG_CHANGED = "argus:app-config-changed";
